const TASKS = [
    {
        name: "Неправильный формат даты и пропущенная точка с запятой",
        originalCode: `Процедура ПростойПример()

    Перем Дата1;
    Перем Дата2;

    Дата1 = '01011999';
    Дата2 = '20240320';

    Если Дата1 < Дата2 Тогда
        Сообщить("Дата1 раньше, чем Дата2");
    Иначе
        Сообщить("Дата1 позже или равна Дата2");
    КонецЕсли
    
КонецПроцедуры`,
        fullHint: `Процедура ПростойПример()

    Перем Дата1;
    Перем Дата2;

    Дата1 = '19990101';
    Дата2 = '20240320';

    Если Дата1 < Дата2 Тогда
        Сообщить("Дата1 раньше, чем Дата2");
    Иначе
        Сообщить("Дата1 позже или равна Дата2");
    КонецЕсли;
    
КонецПроцедуры`
    },
    {
        name: "Опечатка в имени переменной и отсутствие инициализации",
        originalCode: `Процедура РасчетСуммы()

    Перем Число1, Число2;
    Сумма = Число1 + Чило2;

    Сообщить("Сумма равна: " + Сумма);
    
КонецПроцедуры`,
        fullHint: `Процедура РасчетСуммы()

    Перем Число1 = 10, Число2 = 20;
    Сумма = Число1 + Число2;

    Сообщить("Сумма равна: " + Сумма);
    
КонецПроцедуры`
    },
    {
        name: "Несоответствие скобок и ошибка в условии",
        originalCode: `Функция ПроверитьЧисло(Число)

    Если Число > 0 И Число < 100 Тогда
        Возврат "Число в диапазоне";
    ИначеЕсли Число <= 0 Тогда
        Возврат "Отрицательное или ноль";
    Иначе
        Возврат "Большое число";
    КонецЕсли
    
КонецФункции`,
        fullHint: `Функция ПроверитьЧисло(Число)

    Если Число > 0 И Число < 100 Тогда
        Возврат "Число в диапазоне";
    ИначеЕсли Число <= 0 Тогда
        Возврат "Отрицательное или ноль";
    ИначеЕсли Число >= 100 Тогда
        Возврат "Большое число";
    КонецЕсли;
    
КонецФункции`
    },
    {
        name: "Ошибка в цикле и отсутствие точки с запятой",
        originalCode: `Процедура ВыводЧисел()

    Для Счетчик = 1 По 5 Цикл
        Сообщить(Счетчик)
    КонецЦикла
    
КонецПроцедуры`,
        fullHint: `Процедура ВыводЧисел()

    Для Счетчик = 1 По 5 Цикл
        Сообщить(Счетчик);
    КонецЦикла;
    
КонецПроцедуры`
    },
    {
        name: "Неправильное использование типов данных",
        originalCode: `Процедура ПреобразованиеТипов()

    Перем Текст = "123";
    Перем Число = Текст + 10;

    Сообщить("Результат: " + Число);
    
КонецПроцедуры`,
        fullHint: `Процедура ПреобразованиеТипов()

    Перем Текст = "123";
    Перем Число = Число(Текст) + 10;

    Сообщить("Результат: " + Число);
    
КонецПроцедуры`
    },
    {
        name: "Пропущенный оператор присваивания и логическая ошибка",
        originalCode: `Процедура ПроверкаУсловий()

    Перем А = 5, Б = 10;
    Результат;

    Если А > Б Тогда
        Результат = "А больше Б";
    Иначе
        Результат = "Б больше или равно А";
    КонецЕсли

    Сообщить(Результат);
    
КонецПроцедуры`,
        fullHint: `Процедура ПроверкаУсловий()

    Перем А = 5, Б = 10;
    Перем Результат = "";

    Если А > Б Тогда
        Результат = "А больше Б";
    Иначе
        Результат = "Б больше или равно А";
    КонецЕсли;

    Сообщить(Результат);
    
КонецПроцедуры`
    },
    {
        name: "Ошибка в вызове функции и отсутствие возврата значения",
        originalCode: `Функция Умножить(А, Б)

    Произведение = А * Б;

    
КонецФункции

Процедура ОсновнойКод()
    Результат = Умножить(5, 6);
    Сообщить("Результат умножения: " + Результат);
КонецПроцедуры`,
        fullHint: `Функция Умножить(А, Б)

    Произведение = А * Б;
    Возврат Произведение;
    
КонецФункции;

Процедура ОсновнойКод()
    Результат = Умножить(5, 6);
    Сообщить("Результат умножения: " + Результат);
КонецПроцедуры`
    },
    {
        name: "Синтаксическая ошибка в условии и опечатка",
        originalCode: `Процедура АнализДанных()

    Перем Значение = 42;

    Если Значение = 42 Тога
        Сообщить("Значение равно 42");
    Иначи
        Сообщить("Значение не равно 42");
    КонецЕсли
    
КонецПроцедуры`,
        fullHint: `Процедура АнализДанных()

    Перем Значение = 42;

    Если Значение = 42 Тогда
        Сообщить("Значение равно 42");
    Иначе
        Сообщить("Значение не равно 42");
    КонецЕсли;
    
КонецПроцедуры`
    },
    {
        name: "Ошибка в работе с массивом",
        originalCode: `Процедура РаботаСМассивом()

    Массив = Новый Массив;
    Массив.Добавить(1);
    Массив.Добавить(2);
    Массив.Добавить(3);

    Для Индекс = 0 По Массив.Количество Цикл
        Сообщить("Элемент: " + Массив[Индекс]);
    КонецЦикла
    
КонецПроцедуры`,
        fullHint: `Процедура РаботаСМассивом()

    Массив = Новый Массив;
    Массив.Добавить(1);
    Массив.Добавить(2);
    Массив.Добавить(3);

    Для Индекс = 0 По Массив.Количество() - 1 Цикл
        Сообщить("Элемент: " + Массив[Индекс]);
    КонецЦикла;
    
КонецПроцедуры`
    },
    {
        name: "Комплексная ошибка: синтаксис, логика и типы данных",
        originalCode: `Процедура СложныйПример()

    Перем Строка = "100";
    Перем Число = 50;
    Результат = Строка + Число;

    Если Результат > 150 Тогда
        Сообщить("Результат больше 150");
    КонецЕсли

    Для Сч = 1 До 3 Цикл
        Сообщить("Итерация: " + Сч);
    КонецЦикла
    
КонецПроцедуры`,
        fullHint: `Процедура СложныйПример()

    Перем Строка = "100";
    Перем Число = 50;
    Результат = Число(Строка) + Число;

    Если Результат > 150 Тогда
        Сообщить("Результат больше 150");
    КонецЕсли;

    Для Сч = 1 По 3 Цикл
        Сообщить("Итерация: " + Сч);
    КонецЦикла;
    
КонецПроцедуры`
    }
];